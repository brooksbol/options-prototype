# Test configuration — use in-memory SQLite for all tests
evidence.db.path=:memory:

# Disable automatic universe seeding in tests
universe.seed.path=

# Test credential (satisfies startup validation — not a real key)
tradier.api-key=test-key-for-spring-context